#include "../../Flutter/Flutter-Release.xcconfig"
#include "Warnings.xcconfig"

// Keep local DMG signatures free of team-bound entitlements so the regular
// macOS Keychain remains available without an Apple developer certificate.
CODE_SIGN_INJECT_BASE_ENTITLEMENTS = NO
